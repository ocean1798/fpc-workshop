# 26.7.3

## 新增

1. 线路温升 I-W 图表双模式：根据锁定参数自动切换 Y 轴 — ΔT 锁定时显示 W vs I（等温升曲线），W 锁定时显示 ΔT vs I（等线宽曲线）
2. 自定义铜厚输入框与 radio 按钮融合：「自定义」标签内嵌数字输入，不再分两行显示

## 变更

1. Radio 按钮组间距增大 + 标签加水平内边距（`gap: 8px; padding: 5px 8px`），文字不再紧贴边框

## 修复

1. 移除 Google Fonts `@import`（`fonts.googleapis.com` 在国内被墙，CSS `@import` 阻塞渲染导致页面加载超时 30-60 秒），改用系统本地等宽字体 fallback
2. CSS 选择器精确化（`.pi-radio-option > input`），避免自定义铜厚数字输入框被误隐藏

# 26.7.2

## 新增

1. 线圈阻值页面新增 SVG 原理示意图：蛇形走线俯视图 + 截面 A-A 图，标注 W / t / L 尺寸和 R = ρ·L/(t·W) 公式
2. 过孔电流页面新增 SVG 原理示意图：过孔截面图（筒壁导电模型） + 俯视图（环形焊盘），标注 D / t 尺寸和 IPC-2221 公式
3. 线圈阻值 / 过孔电流页面改为左右两栏 Grid 布局（48fr 52fr）：示意图左栏，参数表单右栏

## 变更

1. 免责声明样式强化：蓝色 ℹ️ → 橙色 ⚠️ 警告框 + 左侧粗边框（`border-left: 3px solid var(--warning)`）
2. 左右布局页面免责声明平铺全宽（从右栏移出到 Grid 容器下方）

## 修复

1. 过孔电流公式修正：过孔系数 k=0.02（与嘉立创 FPC 在线计算器一致，此前误用外层 k=0.048）
2. 线圈阻值单位修正：线长计算修复 1000× 单位误差（ρ 单位为 Ω·mm²/m，L 输入单位为 mm）
3. 线路温升铜厚按钮排序修复：0.33oz → 0.5oz → 1.0oz → 2.0oz（从小到大）
4. 过孔示意图标注放大、去除冗余文字（顶层铜/底层铜/焊盘图例等）、避免文字线条重叠

# 26.7.1

## 新增

1. 线路温升 v4 — 验证模式 + I-W 折线图 + 画布集成
2. 画布轮询：选中走线自动读取线宽 W / 线长 L / 层别 / 网络，累计总长并读取网络总长
3. I-W SVG 折线图：横轴 I(A) 纵轴 W(mm)，单条等温升曲线，可拖动圆点实现 I↔W 联动
4. 线宽滑条：手动微调线宽，实时反算实际温升 ΔT
5. 应用线宽到画布：批量修改选中走线线宽（pcb_PrimitiveLine.modify），带确认对话框
6. 右面板重构为数据看板 + 卡片分区：「画布选择」「参数」「结果」三张独立卡片

## 变更

1. 移除 v3 的 HTML 走线截面 + 温度计 SVG + 数据标签
2. 布局从 45/55 调整为 48/52（为 I-W 图表留更多空间）
3. IPC-2221 公式移到左侧图表标题栏 + 右侧参数卡片内

# 26.6.24

## 修复

1. 修复线路温升电阻公式单位错误：多余的 ×1000 导致结果偏大 1000 倍
2. 删除台灯装饰性 SVG 元素（灯座、灯臂、灯罩、LED、光锥、光池），精简为 HTML+CSS 走线截面 + 温度计 SVG
3. 布局重排：左 45% 走线截面大图（铜色矩形 + W/t 标注 + IPC-2221 公式 + 数据标签），右 55% 温度计 + 参数 + 结果卡片
4. 铜厚 radio 改为单行等宽排列（flex: 1），选中态蓝色边框高亮
5. 必填标记 `*` 从红色改为灰色（`var(--text-caption)`）
6. 结果区域重新设计：线宽大号蓝色卡片突出，R/Vd/P 3 列迷你卡片展示
7. 字体层级统一：标题 16px bold，标签 13px，输入 14px，结果 14px bold，线宽 22px bold

# 26.6.23

## 变更

1. 线路温升示意图全新设计：Apple 风格扁平台灯场景替代旧电路图
2. 台灯场景：灯座 → 灯臂 → 灯罩 → LED 灯泡完整台灯结构，光锥 + 桌面光池扩散
3. LED 灯泡光晕（Apple Yellow #FFD60A）随电流 I 对数增强，光池半径和亮度同步扩散
4. 温度计（胶囊形汞柱）替代走线变色：Apple 系统色板渐变 绿→橙→红→粉红，汞柱高度线性对应 ΔT
5. 走线截面 + 数据标签保留，W 动态标注匹配实际线宽

# 26.6.22

## 新增

1. 线路温升标签页新增交互式电路示意图：电池 → PCB 走线 → LED 完整回路，SVG 动态渲染
2. LED 发光随电流 I 对数增强（暗红 → 亮橙 → 强白光晕），走线颜色随温升 ΔT 渐变（铜色 → 暖橙 → 红热临界），热波纹随温升淡入浮动
3. 走线长度 L 驱动走线 bar 物理伸缩，LED / GND 元件自动跟随位移，CSS transition 平滑过渡
4. 走线截面标注同步显示计算宽度 W、铜厚 t、截面面积 A，IPC-2221 公式内嵌
5. 布局改为左右分栏（`tc-calculator-layout`），与压 PAD / 补强厚度标签页风格统一

# 26.6.21

## 新增

1. PI 补强表格支持多选：`selectedStiffener`（单选）→ `selectedStiffeners`（数组），checkbox + 行点击 toggle，批量应用
2. 材质名称中文化 + i18n 基础设施：表格材质列显示（PI / FR4 / 钢片），新增 `SYS_I18n.text()` 调用及硬编码回退
3. 画布选中恢复：应用写回后自动在画布重新选中补强图元（`setDocumentSource` 重渲染导致选中丢失）

## 修复

1. 修复"应用"后表格不刷新：轮询定时器竞态导致旧数据覆盖，改用 `try-finally` 锁守卫全部异步操作
2. 修复深色模式下黑色 / 白色覆盖膜色块不可见：添加白色边框环（`data-theme="dark" .pi-dot { border-color }`）
3. 修复红色"无推荐"气泡尺寸不一致：统一大小与绿色建议气泡相同
4. 修复极限厚度（2.0mm+ PI）时 2D 截面图撑爆整个面板：`html`/`body` 添加 `height:100%; overflow:hidden` 从根约束布局链，`.pi-scene-viewport` 改为 `overflow-y:auto` 承载滚动条

## 变更

1. 引擎最低版本要求：`^2.3.0` → `>=3.2.152`
2. 左侧导航标签优化：PI补强 → 补强厚度，耐电流 → 线路温升，过孔 → 过孔电流
3. 补强表格排序改为层优先（顶层→底层），层内按图元 ID 稳定排序，材质/厚度修改不影响行顺序

# 26.6.20

## 修复

1. 修复钢片材质写入失败：材质标识从 `Steel` 修正为 `Stainless Steel`（与 EDA 引擎内部存储一致，26.6.18 的修复不完备）
2. 修复覆盖膜厚度/颜色选中态样式不统一：补充 `background`、`color`、`font-weight` 过渡效果，与其他 radio 组件一致
3. 修复标尺左侧图例文字垂直对齐偏差：建议厚度 / 可选厚度 / 精准厚度三行图例重新校准 `top` 位置
4. 修复密集刻度标签重叠（如 FR4 0.1/0.2）：刻度标签奇偶行交错渲染
5. 修复精准厚度气泡与刻度标签重叠：气泡下移避开第二行标签
6. "无推荐" 提示升级为红色气泡 + 白色文字，与绿色建议气泡、灰色理论气泡风格统一

# 26.6.19

## 变更

1. PI 补强推荐策略升级为公差优先级：正公差 +0.03mm 优先（过盈配合）→ 负公差 -0.01mm → 无推荐
2. PI 计算器标尺新增左侧图例：🟢 建议厚度 / ┊ 可选厚度 / ⬤ 精准厚度，颜色与标尺元素一一对应
3. 精准厚度气泡与建议厚度指示器融合到同一标尺，直观展示"需求值 vs 建议值"关系
4. 标尺溢出裁切修复：overflow visible + 间距重新计算，刻度/气泡不再遮挡
5. CSS 变量全面迁移：所有 `rgba(94,106,210,...)` 硬编码替换为 `rgba(var(--accent-rgb),...)`，自动适配暗/亮双主题

## 修复

1. 移除 `.glass-panel` 嵌套视觉边框，消除窗口内"框中框"效果
2. 暗/亮双主题背景统一：`--panel-bg` / `--card-diagram-bg` / `--card-control-bg` / `--input-bg` 合并为相同色值

## 优化

1. 材质选择与标尺布局调整：标尺上移（原 PI 补强厚度位置），材质选择器下移
2. 冗余"建议厚度"节标签移除，信息整合到图例

# 26.6.18

## 修复

1. 修复覆盖膜厚度/颜色选项文字与色块分两行显示的问题（CSS 选择器范围过宽导致嵌套 label 被污染）
2. 修复"应用"按钮写回补强后自动切换到另一个补强的问题（新增 write-back 冷静期守卫）
3. 修复钢片材质无法应用成功的问题（`钢片` → `Stainless Steel`，对齐 EDA 引擎内部物料标识，之前版本误写为 `Steel` 导致 EDA 无法识别）

# 26.6.17

## 新增

1. PI 补强计算器 2D 层叠截面可视化：7 层 FPC 结构按比例渲染，支持 layer 开关、hover 高亮
2. 画布补强自动识别：选中补强板 → 读取外形 → 检测对应铜层内容 → 联动"有铜/无铜"
3. 补强列表选择器：列出所有可操作补强，支持点击切换

## 变更

1. PI 计算器数据模型从旧 `FPC_PRESETS` 迁移到 `BOARD_DATA` + `MATERIALS` 新结构
2. 双面板厚预设扩展至 6 种，单面扩展至 4 种
3. 参数面板 select 下拉框全部替换为 radio 单选组 / grid picker
4. 材质厚度选择改为可视化标尺（ruler），标注标准值和建议值

## 修复

1. 修复杂散厚度网格双击选中文字的问题（添加 `no-select` 样式）

# 26.6.16

## 新增

1. 第一个 Tab 压PAD 上线：多焊盘联合扩阻焊 / NSMD ↔ SMD 转换
2. 预处理模式：焊盘先扩再压阻焊，增强 FPC 焊盘结合力
3. 焊盘列表 UI + 参数面板 + 操作预览

# 26.6.1

## 新增

1. 辅助计算 Tab：走线电流、线圈电阻、过孔电流计算器

# 26.6.0

初始版本，基于 pro-api-sdk 搭建项目骨架
